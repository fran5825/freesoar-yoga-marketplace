services:
  postgres:
    image: postgres:16
    container_name: freesoar-yoga-marketplace-postgres
    environment:
      POSTGRES_DB: freesoar_yoga_marketplace_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
