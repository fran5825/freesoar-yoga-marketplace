# 07: 需求表單區塊化與缺項提示（決策 4）

**What to build:** 需求表單維持單頁，但欄位分成 3 個卡片區塊，頂端顯示「還缺 N 項才能送審」，可以點缺項跳到該欄位。草稿機制不變。

**Blocked by:** 01、06（同一個表單檔案，避免衝突）

**Status:** done（2026-09-25）

**Workflow mode:** STANDARD

**Human Gate:** no

**Risk flags:** 不改欄位定義與驗證規則，只改排版與提示

**實作紀錄：** 已完成（範圍縮小）。表單本來就已分成三個卡片區塊並有缺項清單，所以這張只補「還缺 N 項才能送審」計數與「可以送出審核」正向提示；沒有做點擊缺項跳到欄位。

**Source:** `docs/organizer-usability-plan.md`

- [x] 欄位分成 3 個卡片區塊，順序符合填寫直覺
- [x] 頂端顯示缺項數量與清單，欄位補齊後即時更新
- [x] 手機版無橫向捲動、按鈕好按
- [x] 草稿儲存、送審、線上課程勾選等既有行為不變
- [x] `organizer-demand` smoke 測試桌機與手機通過
