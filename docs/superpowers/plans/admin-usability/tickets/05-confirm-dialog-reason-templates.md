# 05: 共用確認視窗與退回原因範本（決策 10、13）

**What to build:** 危險操作（暫停老師、取消整堂課、取消報名）按下後先跳確認視窗，說明後果，原因維持必填；退回原因輸入框旁有 2–3 個範本可一鍵帶入再修改。通過、發布、恢復維持一鍵。

**Blocked by:** 04

**Status:** done（2026-09-26；範本文字待 Franz 確認）

**Workflow mode:** STANDARD

**Human Gate:** no

**Risk flags:** 共用元件；範本文字只帶入輸入框，不改資料結構。範本內容由 Claude 草擬、Franz 確認後定案

**Source:** `docs/admin-usability-plan.md`

- [x] 確認視窗可用鍵盤操作，取消不送出
- [x] 原因空白時不可送出
- [ ] 老師、需求各一組範本文字，Franz 確認過（**草稿已寫好，等 Franz 確認**）
- [x] 桌機與手機可用

**實作紀錄：** 新增 `AdminConfirmButton`（客戶端元件，用瀏覽器內建 `<dialog>`：Esc、點背景、「返回」都不會送出；初始焦點在「返回」；放在 `<form>` 裡，先做表單欄位檢查再跳視窗；標題 id 用 `useId`，同頁多個視窗不撞號）與 `ReasonTemplateField`（範本按鈕點一下帶入、可再改，顯示已輸入字數與 10–1000 字範圍；伺服器端長度驗證不變）。範本草稿在 `_components/reason-templates.ts`（老師、需求各 3 句）。**為了實際驗證，先套用在：** 課程詳情頁的「取消這筆報名」「取消課程」（原本「展開＋勾選確認＋按鈕」三步，改成「按鈕＋確認視窗」兩步；伺服器端的 `confirmCancel` 檢查保留，由表單帶隱藏欄位）、以及老師、需求兩頁現有的退回原因輸入框。所以票 08 剩下的只有頁寬與中文化，票 06、07 重寫詳情頁時直接搬過去。暫停老師的確認視窗留給票 06。測試：`admin-class-session-management` 改寫取消流程（返回、Esc、確認），`admin-teachers` 新增範本帶入與修改；5 支 admin 測試共 62 支通過（`PORT=3100`）。
