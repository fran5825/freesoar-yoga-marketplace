# 02: 老師頁版面統一（決策 1）

**What to build:** 老師申請頁與所有老師頁的頁寬、標題、卡片樣式跟團主端一致。申請頁（`/teachers/join`）改成登入後套用與 `max-w-4xl` 一致的外框；需求池、可授課時間、老師資料頁只統一版面，不改流程。

**Blocked by:** 01

**Status:** done（2026-09-25）

**Workflow mode:** STANDARD

**Human Gate:** no

**Risk flags:** 無（只動版面；申請頁未登入的訪客導覽內容可保留原寬度，需在實作時確認）

**實作紀錄：** 老師頁標題上方的英文小字（Teacher demands、Teacher profile 等 13 處）全部拿掉、標題間距與字級對齊團主頁（詳情頁標題 2xl 改 3xl）；申請頁已登入時頁寬改 `max-w-4xl`，未登入的導覽頁維持 5xl；申請頁保留公開 header（它是公開網址，不在 `/teacher/*` 底下，不套老師導覽列）。申請頁大標題是 36～48px，比其他頁 30px 大，這是刻意保留的迎賓式標題。量測：桌機 7 頁主內容寬度都是 896px、手機無橫向捲動。附帶發現：`teacher-profile-edit` 的「最後更新時間」測試在整批跑時偶發失敗、單獨重跑通過，跟版面無關，未處理。

**Source:** `docs/teacher-usability-plan.md`

- [x] 已登入看申請頁，頁寬與導覽列和其他老師頁一致
- [x] 各老師頁標題層級、卡片圓角與間距與團主端一致
- [x] 手機（375 寬）沒有橫向捲動
- [x] 老師相關 smoke 測試桌機與手機通過
